# 第一周（数据模型 + 调度引擎）
pydantic>=2.0
pytest>=8.0

# 第二周起（节点运行时 + API）
fastapi>=0.110
uvicorn[standard]>=0.30
sqlalchemy>=2.0
httpx>=0.27
openai>=1.30

# 第四周起（前端画布）
# react  @xyflow/react  vite  typescript   -> 见 frontend/package.json
